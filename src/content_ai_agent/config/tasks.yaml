# default agent-task
# research_task:
#   description: >
#     Conduct a thorough research about {topic}
#     Make sure you find any interesting and relevant information given
#     the current year is {current_year}.
#   expected_output: >
#     A list with 10 bullet points of the most relevant information about {topic}
#   agent: researcher

# reporting_task:
#   description: >
#     Review the context you got and expand each topic into a full section for a report.
#     Make sure the report is detailed and contains any and all relevant information.
#   expected_output: >
#     A fully fledged report with the main topics, each with a full section of information.
#     Formatted as markdown without '```'
#   agent: reporting_analyst

#custom agent-task
find_trending_topics:
  description: >
    If a specific topic is provided: "{topic}" - search YouTube for the best performing content about this exact topic.
    If no topic provided: Find 5 trending topics in {niche} that have viral potential.
    Use YouTube Trending Search tool to find real data.
  expected_output: >
    List of 5 best content ideas with: title, why it works, key angles to cover
  agent: topic_finder


research_content:
  description: >
    Research the selected topic: {topic}
    Gather key facts, statistics, expert opinions, and audience pain points.
  expected_output: >
    Structured research doc with: key points, stats, quotes, hooks, audience insights
  agent: content_researcher

write_script:
  description: >
    Create a {platform} script for: {topic}
    Include strong hook (first 3 sec), engaging body, clear CTA.
  expected_output: >
    Ready-to-record script with: hook, main content, CTA, suggested visuals/B-roll
  agent: script_writer